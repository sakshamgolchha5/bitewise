/* Runtime config for the hosted app.
   apiBase = '' means "same origin" (the Bitewise server serves both the app and /api).
   For a native (Capacitor) build, set it to your deployed server, e.g. 'https://bitewise.onrender.com' */
window.BITEWISE_CONFIG = { apiBase: '' };
